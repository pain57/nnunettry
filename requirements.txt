# Core — official nnU-Net v2 (https://github.com/MIC-DKFZ/nnUNet)
torch>=2.0
nnunetv2>=2.2,<3.0
batchgenerators>=0.25
nibabel
numpy
scipy
scikit-image
tqdm

# Experiment 6 comparison models (UNETR / SwinUNETR)
monai>=1.3

# MedNeXt (Exp 6, optional) — not on PyPI, install from source:
#   pip install git+https://github.com/MIC-DKFZ/MedNeXt.git
